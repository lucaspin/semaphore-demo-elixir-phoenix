version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    dependencies: []
    run:
      when: change_in('/test')
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - 'for i in $(seq 1 900); do echo "[$i] Hello, hello, hello" && sleep 1; done'
promotions:
  - name: Promotion 1
    pipeline_file: pipeline_2.yml
