version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e2-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    dependencies: []
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo $SEMAPHORE_ARTIFACT_TOKEN
            - echo test > /tmp/test.txt
            - artifact push project /tmp/test.txt -f
            - artifact push workflow /tmp/test.txt -f
            - artifact push job /tmp/test.txt
promotions:
  - name: Promotion 1
    pipeline_file: pipeline_2.yml
    deployment_target: lucaspin-testing
